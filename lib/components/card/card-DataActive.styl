/**  INTERACCIONES .card::after
 **------------------------------------------*/

//card desactivado
.card[data-active="inactive"]::after
  background-color rgba($default-primary-color, .6)
  left 10px
  top 10px

//card activado con clic
.card[data-active="active"]::after
  background-color rgba($default-primary-color, 1)
  left 16px
  top 40px

//card activado con hover por encima de 1025px
@media screen and (min-width: 1025px)
  .card[data-active="inactive"]:hover::after
    background-color rgba($default-primary-color, 1)
    left 16px
    top 40px

/**  INTERACCIONES .card__btn
 **------------------------------------------*/

//card__btn desactivado
.card[data-active="inactive"] > .card__btn::after
  animation icon-more .3s linear 0s 1 normal forwards
  content '\f19b' // \f19b
  opacity 0
  transform rotate(0)
  visibility hidden

//card__btn activado
.card[data-active="active"] > .card__btn::after
  animation icon-close .3s linear 0s 1 normal forwards
  content '\f136' // \f136
  opacity 0
  transform rotate(0)
  visibility hidden

/**  INTERCCIONES .social
 **------------------------------------------*/
.card[data-active="inactive"] > .social
  height 0
  visibility hidden

.card[data-active="active"] > .social
  height 40px
  visibility visible

//.social activado con hover por encima de 1025px
@media screen and (min-width: 1025px)
  .card[data-active="inactive"]:hover > .social
    height 40px
    visibility visible

/**  INTERACCIONES .social__link
 **------------------------------------------*/
.card[data-active="inactive"] > .social >.social__link
  border-radius 50%
  transform scale(0)

.card[data-active="active"] > .social >.social__link
  border-radius 0
  transform scale(1)

//.social__link activado con hover por encima de 1025px
@media screen and (min-width: 1025px)
  .card[data-active="inactive"]:hover > .social >.social__link
    border-radius 0
    transform scale(1)
